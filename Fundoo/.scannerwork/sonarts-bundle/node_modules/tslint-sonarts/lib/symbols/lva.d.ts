import * as ts from "typescript";
import { SymbolTable } from "./table";
export declare class LiveVariableAnalyzer {
    private readonly symbols;
    private blockAvailableReads;
    private root;
    constructor(symbols: SymbolTable);
    analyze(root: ts.FunctionLikeDeclaration): void;
    private computeSymbolsWithAvailableReads(block);
    private trackUsage(usage, availableReads);
    private isUsedInNestedFunctions(symbol);
    private successorSymbolsWithAvailableReads(block);
    private same(newAvailableReads, oldAvailableReads?);
}
